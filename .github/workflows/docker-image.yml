name: Hola Mundo


on:
  push:
  workflow_dispatch:


jobs:

  SAST:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Run a one-line script
        run: ls ${{ github.workspace }}
